using System.Text;

namespace ConsoleApp1
{
    internal class Program
    {
        static void Main(string[] args)
        {/*
            List<type> name = Console.ReadLine()
                .Split()
                .Select(type.Parse)
                .ToList();
            *****************************************************************
            string input = default;
            while ((input = Console.ReadLine()) != "end")
            {
                string[] inputTokens = input.Split().ToArray();
                string command = inputTokens[0];
                if ($command == "type"$)
                {

                }
                else if (command == name)
            }
            ******************************************************************
            Console.WriteLine(string.Join(' ', listOrArr));

            ******************************************************************
          
            Dictionary<string,string> name = new Dictionary<string,string>();

          ********************************************************************
          
            string input = Console.ReadLine();
            Dictionary<char, int> chars = new Dictionary<char, int>();

            foreach (char character in input.Where(c => c != ' '))
            {
                if (!chars.ContainsKey(character))
                {
                    chars.Add(character, 0);
                }

                chars[character]++;
            }

            foreach (var kvp in chars)
            {
                Console.WriteLine($"{kvp.Key} -> {kvp.Value}");
            }
            ****************************************************************
                StringBuilder sb = new StringBuilder();
                sb.AppendLine();


            */



        }
    }
}
